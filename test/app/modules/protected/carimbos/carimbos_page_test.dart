import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:digifidelidade/app/modules/protected/carimbos/carimbos_page.dart';

main() {
  testWidgets('CarimbosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CarimbosPage(title: 'Carimbos')));
    //  final titleFinder = find.text('Carimbos');
    //  expect(titleFinder, findsOneWidget);
  });
}
