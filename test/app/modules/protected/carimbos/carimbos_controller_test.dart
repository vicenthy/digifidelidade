import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:digifidelidade/app/modules/protected/carimbos/carimbos_controller.dart';
import 'package:digifidelidade/app/modules/protected/protected_module.dart';

void main() {
  initModule(ProtectedModule());
  // CarimbosController carimbos;
  //
  setUp(() {
    //     carimbos = ProtectedModule.to.get<CarimbosController>();
  });

  group('CarimbosController Test', () {
    //   test("First Test", () {
    //     expect(carimbos, isInstanceOf<CarimbosController>());
    //   });

    //   test("Set Value", () {
    //     expect(carimbos.value, equals(0));
    //     carimbos.increment();
    //     expect(carimbos.value, equals(1));
    //   });
  });
}
