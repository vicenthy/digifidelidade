import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:digifidelidade/app/modules/protected/protected_module.dart';

void main() {
  initModule(ProtectedModule());
  // TabCarimbarController tabcarimbar;
  //
  setUp(() {
    //     tabcarimbar = ProtectedModule.to.get<TabCarimbarController>();
  });

  group('TabCarimbarController Test', () {
    //   test("First Test", () {
    //     expect(tabcarimbar, isInstanceOf<TabCarimbarController>());
    //   });

    //   test("Set Value", () {
    //     expect(tabcarimbar.value, equals(0));
    //     tabcarimbar.increment();
    //     expect(tabcarimbar.value, equals(1));
    //   });
  });
}
