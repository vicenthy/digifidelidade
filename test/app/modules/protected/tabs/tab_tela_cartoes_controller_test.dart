import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:digifidelidade/app/modules/protected/tabs/tab_tela_cartoes_controller.dart';
import 'package:digifidelidade/app/modules/protected/protected_module.dart';

void main() {
  initModule(ProtectedModule());
  // TabTelaCartoesController tabtelacartoes;
  //
  setUp(() {
    //     tabtelacartoes = ProtectedModule.to.get<TabTelaCartoesController>();
  });

  group('TabTelaCartoesController Test', () {
    //   test("First Test", () {
    //     expect(tabtelacartoes, isInstanceOf<TabTelaCartoesController>());
    //   });

    //   test("Set Value", () {
    //     expect(tabtelacartoes.value, equals(0));
    //     tabtelacartoes.increment();
    //     expect(tabtelacartoes.value, equals(1));
    //   });
  });
}
