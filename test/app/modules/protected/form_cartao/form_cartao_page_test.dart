import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:digifidelidade/app/modules/protected/form_cartao/form_cartao_page.dart';

main() {
  testWidgets('FormCartaoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FormCartaoPage(title: 'FormCartao')));
    //  final titleFinder = find.text('FormCartao');
    //  expect(titleFinder, findsOneWidget);
  });
}
