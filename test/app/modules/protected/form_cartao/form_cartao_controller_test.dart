import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:digifidelidade/app/modules/protected/form_cartao/form_cartao_controller.dart';
import 'package:digifidelidade/app/modules/protected/protected_module.dart';

void main() {
  initModule(ProtectedModule());
  // FormCartaoController formcartao;
  //
  setUp(() {
    //     formcartao = ProtectedModule.to.get<FormCartaoController>();
  });

  group('FormCartaoController Test', () {
    //   test("First Test", () {
    //     expect(formcartao, isInstanceOf<FormCartaoController>());
    //   });

    //   test("Set Value", () {
    //     expect(formcartao.value, equals(0));
    //     formcartao.increment();
    //     expect(formcartao.value, equals(1));
    //   });
  });
}
