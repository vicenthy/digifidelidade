import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:digifidelidade/app/modules/public/login/login_controller.dart';
import 'package:digifidelidade/app/modules/public/public_module.dart';

void main() {
  initModule(PublicModule());
  // LoginController login;
  //
  setUp(() {
    //     login = PublicModule.to.get<LoginController>();
  });

  group('LoginController Test', () {
    //   test("First Test", () {
    //     expect(login, isInstanceOf<LoginController>());
    //   });

    //   test("Set Value", () {
    //     expect(login.value, equals(0));
    //     login.increment();
    //     expect(login.value, equals(1));
    //   });
  });
}
