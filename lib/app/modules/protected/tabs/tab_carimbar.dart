import 'package:flutter/material.dart';

class TabCarimbar extends StatelessWidget {
  const TabCarimbar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(),
    );
  }
}
