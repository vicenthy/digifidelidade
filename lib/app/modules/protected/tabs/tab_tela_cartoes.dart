import 'package:flutter/material.dart';

class TabTelaCartoes extends StatelessWidget {
  const TabTelaCartoes({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(),
    );
  }
}
